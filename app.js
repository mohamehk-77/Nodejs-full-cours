const sayHi = require('./name'); // Ensure the path is correct

sayHi('hamo');
sayHi('moha');
sayHi('sansa');

const items = { items: ['items1', 'items2'], singlePerson: { name: 'John' } };
console.log(items);